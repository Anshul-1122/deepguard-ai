# Base URL for API calls. Leave as /api to use the Vite dev proxy (see vite.config.ts),
# which forwards to http://localhost:8000. In production, point this at your deployed
# backend, e.g. https://api.deepguard.ai/api
VITE_API_BASE_URL=/api
